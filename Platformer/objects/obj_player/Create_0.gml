/// @description Initialises physics settings
y_speed = 0;   // Current y speed of the player
grav = 1;      // Increases speed falling by 1 pixel per frame

WALK_SPEED = 10;  // Pixels to move per frame when walking
JUMP_SPEED = -16; // Pixels to move per frame when jumping